numpy>=1.24
opencv-contrib-python>=4.8
scipy>=1.10
matplotlib>=3.7
flask>=3.0

# Optional (LoFTR primary matcher). Pipeline falls back to SIFT if missing.
torch>=2.0
kornia>=0.7
kornia_moons>=0.2

# Optional (georeferencing metadata for ISSDC Pradan products)
rasterio>=1.3
